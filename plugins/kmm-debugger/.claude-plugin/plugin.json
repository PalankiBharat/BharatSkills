{
  "name": "kmm-debugger",
  "description": "Deep-investigation debugging workflow for KMM post-migration regressions. Hard-gated A/B subagent pair dispatch per topic before any plan or fix, bias guard against defending existing implementation, 'is this even our bug?' as the first question (catches upstream contract violations), Doctrine 3 'always prefer clean long-term solutions over hotfixes / iterative patches', auto-firing retro after each push/PR/publish.",
  "version": "1.1.0"
}
